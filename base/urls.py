from django.urls import path
from .views import *

urlpatterns = [
#========================================================================================================================== 
                                                # Item-Type URL 
#==========================================================================================================================

    path("item-type/", ItemTypeView.as_view({'get':'list', 'post':'create'})),
    path('item-type/<int:pk>', ItemTypeView.as_view({'get':'retrieve', 'put':'update', 'delete':'destroy'})),
    
#========================================================================================================================== 
                                                # Item URL 
#==========================================================================================================================

    path("item/", ItemView.as_view()),
    
#========================================================================================================================== 
                                                # Department URL 
#==========================================================================================================================

    path("department/", DepartmentView.as_view({'get':'list', 'post':'create'})),
    path('department/<int:pk>', DepartmentView.as_view({'get':'retrieve', 'put':'update', 'delete':'destroy'})),
    

#========================================================================================================================== 
                                                # Vendor URL 
#==========================================================================================================================

    path("vendor/", VendorView.as_view())


]